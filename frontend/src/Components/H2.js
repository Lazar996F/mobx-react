import styled from 'styled-components';
import {COLOR_SECONDARY} from '../Utils/styles';

const H2 = styled.h2`
  font-size: 0.8em;
  color: ${COLOR_SECONDARY};
  font-weight: 400;
`;

export default H2;
